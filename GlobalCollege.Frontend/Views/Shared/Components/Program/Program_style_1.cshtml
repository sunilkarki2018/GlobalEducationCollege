@model GlobalCollege.Entity.ViewComponent.ProgramViewComponentModel
@{
    Layout = "";

}


<!-- PROGRAM CAROUSEL  -->
<section id="getStart">
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <h2 class="section-title">Our Programs</h2>
            </div>
        </div>
        <div class="row">
            <div class="col-lg-12">
                <div class="get_start_carousel owl-carousel">
                    @foreach (var item in Model.Programs.OrderBy(x => x.PlacementOrder))
                    {
                        <div>
                            <a href="/ContentManagement/ProgramSetup/Details?Id=@item.Id">
                                <div class="getStart__wrapper wow bounceInUp">
                                    <img src="@item.ThumbnailImageLink" alt="a-level">
                                    <h5>@item.Title</h5>
                                </div>
                            </a>

                        </div>
                    }

                </div>
            </div>
            <div class="col-md-12 text-center pt-5">
                <a href="/ContentManagement/ProgramSetup/Index" class="btn btn-primary">
                    View All
                    Programs
                </a>

            </div>
        </div>
    </div>
</section>
<!-- PROGRAM CAROUSEL  -->
